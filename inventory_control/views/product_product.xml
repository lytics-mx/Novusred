<odoo>
    <record id="view_product_product_list" model="ir.ui.view">
        <field name="name">product.product.list</field>
        <field name="model">product.product</field>
        <field name="arch" type="xml">
            <list string="Productos">
                <field name="name" string="Nombre del Producto"/>
                <field name="default_code" string="Referencia Interna"/>
                <field name="product_model" string="Modelo del Producto"/>
                <field name="list_price" string="Precio de Venta"/>
                <field name="standard_price" string="Costo"/>
                <field name="qty_available" string="A la Mano"/>
            </list>
        </field>
    </record>
</odoo>
