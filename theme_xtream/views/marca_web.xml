<odoo>
    <template id="products_sidebar" inherit_id="website_sale.products">
        <xpath expr="//div[@class='list-group mt-4'][h5[contains(text(), 'Categories')]]" position="after">
            <!-- Marcas (debajo de categorías) -->
            <div class="list-group mt-4">
                <h5 class="mb-3">Brands</h5>
                <t t-foreach="brands" t-as="brand">
                    <a t-att-href="'/shop?brand=' + str(brand.id)" 
                       t-att-class="'list-group-item list-group-item-action' + (' active' if selected_brand == brand.id else '')">
                        <t t-esc="brand.name"/>
                    </a>
                </t>
            </div>
        </xpath>
    </template>
</odoo>