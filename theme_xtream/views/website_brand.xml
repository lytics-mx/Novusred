<odoo>
    <template id="website_brand" name="Brand Page">
        <t t-call="website.layout">
            <div class="container">                              
                <div class="row">
                    <div class="col-lg-3">
                        <h4>Etiquetas</h4>
                        <ul class="nav flex-column">
                            <t t-foreach="request.env['product.tag'].search([])" t-as="tag">
                                <li class="nav-item">
                                    <div class="form-check">
                                        <input type="checkbox" 
                                               class="form-check-input tag-checkbox" 
                                               t-att-value="tag.id"
                                               t-att-checked="str(tag.id) in request.params.get('tags', '').split(',')"
                                               t-att-id="'tag_%s' % tag.id"/>
                                        <label class="form-check-label" t-att-for="'tag_%s' % tag.id">
                                            <t t-esc="tag.name"/>
                                        </label>
                                    </div>
                                </li>
                            </t>
                        </ul>
                    </div>
                    <div class="col-lg-9">
                        <section class="s_dynamic_snippet_products" 
                            t-att-data-filter-id="{'tag_ids': request.params.get('tags').split(',') if request.params.get('tags') else []}"
                            data-number-of-records="16"
                            data-template-key="website_sale.dynamic_filter_template_product_product_borderless_1">
                        <div class="dynamic_snippet_template"/>
                    </section>

                    </div>
                </div>
            </div>
        </t>
    </template>
</odoo>
