<odoo>
    <record id="banner_images_form" model="ir.ui.view">
        <field name="name">banner.images.form</field>
        <field name="model">banner.images</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                    </group>
                    <notebook>
                        <page string="Imágenes Generales">
                            <field name="general_images" widget="many2many_binary"/>
                        </page>
                        <page string="Imágenes de Productos">
                            <field name="product_images" widget="many2many_binary"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>
</odoo>