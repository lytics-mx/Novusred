<odoo>
    <template id="viewed_products_template" name="Productos Vistos">
        <div class="viewed-products" style="margin-top: 30px;">
            <h3 style="text-align: center;">Productos que has visto</h3>
            <div class="o_wsale_products_grid_table_wrapper pt-3 pt-lg-0">
                <section id="o_wsale_products_grid" class="o_wsale_products_grid_table grid">
                    <!-- Itera sobre los productos visitados -->
                    <t t-foreach="request.env['product.template'].sudo().browse(request.session.get('viewed_products', []))" t-as="product">
                        <div class="oe_product g-col-6 g-col-md-4 g-col-lg-3">
                            <div class="o_wsale_product_grid_wrapper position-relative h-100">
                                <!-- Llama al template de productos de la tienda -->
                                <t t-call="website_sale.products_item">
                                    <t t-set="product" t-value="product" />
                                </t>
                            </div>
                        </div>
                    </t>
                </section>
            </div>
        </div>
    </template>
</odoo>