<odoo>
    <record id="view_product_template_tree_offers" model="ir.ui.view">
        <field name="name">product.template.tree.offers</field>
        <field name="model">product.template</field>
        <field name="arch" type="xml">
            <tree string="Productos en Oferta">
                <field name="name" />
                <field name="default_code" string="Referencia" />
                <field name="list_price" string="Precio de Lista" />
                <field name="discount_percentage" string="Descuento (%)" />
                <field name="discounted_price" string="Precio con Descuento" />
                <field name="website_published" string="Publicado en Web" />
            </tree>
        </field>
    </record>
</odoo>