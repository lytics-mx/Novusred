<odoo>
    <record id="view_product_template_form_inherit" model="ir.ui.view">
        <field name="name">product.template.form.inherit</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='public_categ_ids']" position="after">
                <!-- Campo para seleccionar la etiqueta de producto -->
                <field name="product_tag_id" />
                
                <!-- Campo relacionado: porcentaje de descuento desde la etiqueta -->
                <field name="discount_percentage" readonly="1" />
                
                <!-- Campo calculado: precio con descuento -->
                <field name="discounted_price" readonly="1" />
            </xpath>
        </field>
    </record>
</odoo>